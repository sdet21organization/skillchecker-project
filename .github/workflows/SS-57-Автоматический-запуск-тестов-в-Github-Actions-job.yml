name: Maven Tests with Allure

on:
  push:
    branches:
      - main
      - feature/**
  pull_request:
    branches:
      - main

jobs:
  api-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # чтобы подтянуть ветку gh-pages тоже

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run API Tests
        run: mvn clean test -f api-tests/pom.xml -Dmaven.test.failure.ignore=true

      - name: Upload Allure Results (API)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-api
          path: api-tests/target/allure-results

  ui-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # Обновлено до v4 для единообразия
        with:
          fetch-depth: 0 # Добавлено для единообразия и возможных будущих нужд

      - uses: actions/setup-java@v4 # Обновлено до v4 для единообразия
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run UI Tests
        run: mvn clean test -f ui-tests/pom.xml -Dmaven.test.failure.ignore=true

      - name: Upload Allure Results (UI)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-ui
          path: ui-tests/target/allure-results


  allure-report:
   needs: [ api-tests, ui-tests ]
   runs-on: ubuntu-latest
   if: always()
   steps:
    - uses: actions/checkout@v4 # Обновлено до v4
      with:
        fetch-depth: 0

    - name: Download Allure Results (API)
      uses: actions/download-artifact@v4
      with:
        name: allure-results-api
        path: allure-results/api

    - name: Download Allure Results (UI)
      uses: actions/download-artifact@v4
      with:
        name: allure-results-ui
        path: allure-results/ui

    - name: Merge Allure Results
      run: |
        mkdir -p merged-allure-results
        # Использование find и xargs для копирования содержимого, исключая папки-артефакты,
        # чтобы избежать проблем, если папка пуста
        find allure-results/api -mindepth 1 -maxdepth 1 -exec cp -r {} merged-allure-results/ \; || true
        find allure-results/ui -mindepth 1 -maxdepth 1 -exec cp -r {} merged-allure-results/ \; || true

    - name: Restore Allure History 💾
      uses: actions/checkout@v4 # Используем action для восстановления истории
      with:
        repository: ${{ github.repository }}
        ref: gh-pages
        path: gh-pages-branch
        fetch-depth: 0

    - name: Copy Allure History
      run: |
        BRANCH_NAME="${{ github.head_ref || github.ref_name }}"
        # При пустом github.head_ref (для push в main) используем имя ветки
        if [ -d "gh-pages-branch/${BRANCH_NAME}/history" ]; then
          mkdir -p merged-allure-results/history
          cp -r gh-pages-branch/${BRANCH_NAME}/history/* merged-allure-results/history/
          echo "History restored from gh-pages/${BRANCH_NAME}/history"
        else
          echo "No existing history found for branch: ${BRANCH_NAME}"
        fi

    - name: Generate Allure Report
      uses: simple-elf/allure-report-action@v1.7
      with:
        allure_results: merged-allure-results
        allure_report: allure-report
        # allure_history больше не нужен, т.к. история уже скопирована в allure_results

    - name: Save Allure History
      run: |
        BRANCH_NAME="${{ github.head_ref || github.ref_name }}"
        # Переносим сгенерированную историю в корень отчета для деплоя
        mkdir -p allure-report/history
        cp -r merged-allure-results/history/* allure-report/history/ || true
        # Копируем новую историю для текущей ветки
        mkdir -p allure-report/${BRANCH_NAME}/history
        cp -r allure-report/history/* allure-report/${BRANCH_NAME}/history/

    - name: Deploy Allure Report to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: allure-report
        # Публикуем отчет в корень для main и в подпапки для feature-веток
        destination_dir: ${{ github.head_ref || github.ref_name }}
        # force_orphan: true # Опционально, чтобы не загрязнять gh-pages лишними файлами

    - name: Comment PR with Allure Report Link
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const branchName = context.payload.pull_request.head.ref;
          const repoName = context.repo.repo;
          const owner = context.repo.owner;
          // Убедитесь, что отчет доступен по ссылке: https://owner.github.io/repo/branch-name/
          const reportUrl = `https://${owner}.github.io/${repoName}/${branchName}/`;
          
          await github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner,
            repo: repoName,
            body: `✅ **Allure Report** доступен по ссылке: [открыть отчёт](${reportUrl})`
          });

    - name: Send Discord Notification
      if: always()
      env:
       DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      run: |
            branch="${{ github.head_ref || github.ref_name }}"
            report_url="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/${branch}/"
            
            passed=$(jq '.statistic.passed' allure-report/widgets/summary.json)
            failed=$(jq '.statistic.failed' allure-report/widgets/summary.json)
            broken=$(jq '.statistic.broken' allure-report/widgets/summary.json)
            skipped=$(jq '.statistic.skipped' allure-report/widgets/summary.json)
            total=$(jq '.statistic.total' allure-report/widgets/summary.json)
            
            color=3066993
            
            payload=$(jq -n \
              --arg passed "$passed" \
              --arg failed "$failed" \
              --arg broken "$broken" \
              --arg skipped "$skipped" \
              --arg total "$total" \
              --arg url "$report_url" \
              --arg branch "$branch" \
              --arg repo "${{ github.repository }}" \
              --argjson color "$color" \
              '{
                "embeds": [
                  {
                   "title": "Allure Report",
                   "url": $url,
                   "color": $color,
                   "fields": [
                     {
                      "name": "📁 ",
                      "value": $repo,
                      "inline": true
                     },
                    {
                     "name": "🌿 ",
                     "value": $branch,
                     "inline": true
                    },
                    {
                      "name": "Test Results",
                      "value": "✅ Passed \($passed) ⚠️ Broken \($broken) ❌ Failed \($failed) ⏭️ Skipped \($skipped) 📊 Total \($total)",
                      "inline": false
                     }
                    ]
                  }
                ]
               }')
            
            curl -H "Content-Type: application/json" \
                -d "$payload" \
                "$DISCORD_WEBHOOK_URL"