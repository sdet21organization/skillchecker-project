name: Maven Tests with Allure

on:
  push:
    branches:
      - main
      - feature/**
  pull_request:
    branches:
      - main

jobs:
  api-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # чтобы подтянуть ветку gh-pages тоже

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run API Tests
        run: mvn clean test -f api-tests/pom.xml -Dmaven.test.failure.ignore=true

      - name: Upload Allure Results (API)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-api
          path: api-tests/target/allure-results

  ui-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run UI Tests
        run: mvn clean test -f ui-tests/pom.xml -Dmaven.test.failure.ignore=true

      - name: Upload Allure Results (UI)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-ui
          path: ui-tests/target/allure-results

  allure-report:
    needs: [api-tests, ui-tests]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Checkout Code (for Allure deployment)
        # Pull Request и deploy требуют полную историю для Allure History
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Включен fetch-depth для доступа к истории gh-pages и корректного сравнения веток

      - name: Download Allure Results
        uses: actions/download-artifact@v4
        with:
          # Скачиваем все артефакты в одну корневую папку (создаст поддиректории по именам артефактов)
          path: downloaded-allure-results

      - name: Combine Results
        # Перемещаем содержимое всех загруженных папок в одну общую папку combined-allure-results
        run: |
          mkdir -p combined-allure-results
          # allure-results-api и allure-results-ui - это имена артефактов
          cp -r downloaded-allure-results/allure-results-api/* combined-allure-results/ || true
          cp -r downloaded-allure-results/allure-results-ui/* combined-allure-results/ || true

      - name: Allure Report Generation and GitHub Pages Deployment
        # Используем action для генерации отчета с историей и деплоя на gh-pages
        uses: simple-elf/allure-report-action@v1.7
        with:
          # Папка с объединенными результатами
          allure_results: combined-allure-results
          # Целевая ветка для размещения отчета
          gh_pages_branch: gh-pages
          # Папка внутри gh-pages для сохранения отчета (используем имя текущей ветки)
          report_path: ${{ env.BRANCH_NAME }}
          # Токен для разрешения публикации
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # Включение истории (данные будут браться из предыдущего отчета по пути report_path)
          allure_history: allure-history

      - name: Comment PR with Allure Report Link
        # Запускаем только при Pull Request
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const branchName = process.env.BRANCH_NAME;
            const repoName = context.repo.repo;
            const owner = context.repo.owner;
            // Формируем URL: https://<owner>.github.io/<repo>/<branch-name>/
            const reportUrl = `https://${owner}.github.io/${repoName}/${branchName}/`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner,
              repo: repoName,
              body: `✅ **Отчёт Allure с историей и трендами доступен:** [открыть отчёт](${reportUrl})\n\n*(Отчёт опубликован на ветку \`gh-pages\` в папку \`${branchName}/\`)*`
            });

      - name: Send Discord Notification
        if: github.ref == 'refs/heads/main'
        env:
         DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
            branch="${{ github.head_ref || github.ref_name }}"
            report_url="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/${branch}/"
            
            passed=$(jq '.statistic.passed' allure-report/widgets/summary.json)
            failed=$(jq '.statistic.failed' allure-report/widgets/summary.json)
            broken=$(jq '.statistic.broken' allure-report/widgets/summary.json)
            skipped=$(jq '.statistic.skipped' allure-report/widgets/summary.json)
            total=$(jq '.statistic.total' allure-report/widgets/summary.json)
            
            color=3066993
            
            payload=$(jq -n \
              --arg passed "$passed" \
              --arg failed "$failed" \
              --arg broken "$broken" \
              --arg skipped "$skipped" \
              --arg total "$total" \
              --arg url "$report_url" \
              --arg branch "$branch" \
              --arg repo "${{ github.repository }}" \
              --argjson color "$color" \
              '{
                "embeds": [
                  {
                   "title": "Allure Report",
                   "url": $url,
                   "color": $color,
                   "fields": [
                     {
                      "name": "📁 ",
                      "value": $repo,
                      "inline": true
                     },
                    {
                     "name": "🌿 ",
                     "value": $branch,
                     "inline": true
                    },
                    {
                      "name": "Test Results",
                      "value": "✅ Passed \($passed) ⚠️ Broken \($broken) ❌ Failed \($failed) ⏭️ Skipped \($skipped) 📊 Total \($total)",
                      "inline": false
                     }
                    ]
                  }
                ]
               }')
            
            curl -H "Content-Type: application/json" \
                -d "$payload" \
                "$DISCORD_WEBHOOK_URL"